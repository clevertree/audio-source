import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    default: {
        textAlign: 'center',
        flexDirection:'row',
    },

    arrow: {
        marginLeft: 'auto'
    }
});
